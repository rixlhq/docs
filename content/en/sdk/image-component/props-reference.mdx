---
title: "Props Reference"
description: "Complete API reference for all Image component props and TypeScript interfaces"
---

import {Callout} from "@/snippets";

## Core Props

### `id` **(Required)**

**Type:** `string`

The unique image identifier from your RIXL dashboard. Upload your image to [dash.rixl.com/images](https://dash.rixl.com/images) to get this ID.

```tsx
<Image id="your-image-id" />
```

This is mapped to `imageId` on the underlying `<rixl-image>` web component.

### `alt` **(Accepted for backward compatibility)**

**Type:** `string` | **Default:** `undefined`

The React wrapper accepts `alt` for backward compatibility, but does **not** forward it to the Lit element. Alt text is derived from the fetched `imageData` internally. You do not need to pass `alt` manually.

```tsx
<Image id="your-image-id" alt="A sunset over the mountains" />
```

### `className` **(Optional)**

**Type:** `string` | **Default:** `undefined`

CSS classes for styling the image container. Supports all CSS frameworks including TailwindCSS.

```tsx
<Image id="your-image-id" className="object-contain w-full h-full rounded-lg shadow-md" />
```

## Analytics Props

### `analytics` **(Optional)**

**Type:** `boolean` | **Default:** `true`

Enables built-in view tracking for images. When enabled, the SDK automatically tracks `content_views` `start`/`watch`/`end` events and `image_click` interactions.

```tsx
<Image id="your-image-id" analytics={true} />
```

### `analyticsPage` **(Optional)**

**Type:** `"feed" | "standalone" | "profile"` | **Default:** `"standalone"`

Specifies the page context for analytics reporting.

```tsx
<Image id="your-image-id" analyticsPage="profile" />
```

**Available Contexts:**

- `'standalone'` - Image on its own dedicated page
- `'feed'` - Image within a scrolling feed
- `'profile'` - Image on a user profile page

### `feedId` **(Optional)**

**Type:** `string` | **Default:** `undefined`

Feed identifier when the image is rendered inside a feed. `<Feed>` automatically propagates this value to child images.

```tsx
<Image id="your-image-id" feedId="my-feed" postId="post-123" />
```

### `postId` **(Optional)**

**Type:** `string` | **Default:** `undefined`

Post identifier when the image is rendered inside a feed.

### `isCurrent` **(Optional)**

**Type:** `boolean` | **Default:** `false`

Marks this post as the active viewport item. `<Feed>` sets this automatically on the currently visible post.

### `onRixlAnalytics` **(Optional)**

**Type:** `(event: CustomEvent<{ event: AnalyticsEvent }>) => void`

Called whenever the underlying `<rixl-image>` dispatches a `rixl-analytics` event. Use this to inspect or forward analytics payloads.

```tsx
<Image id="your-image-id" onRixlAnalytics={(e) => console.log(e.detail.event)} />
```

## TypeScript Interface

```typescript
interface ImageProps extends HTMLProps<HTMLImageElement> {
  id?: string;
  alt?: string; // accepted for backward compatibility, not forwarded
  className?: string;
  analytics?: boolean;
  analyticsPage?: Page;
  feedId?: string;
  postId?: string;
  isCurrent?: boolean;
  onRixlAnalytics?: (event: CustomEvent<{event: AnalyticsEvent}>) => void;
}
```
